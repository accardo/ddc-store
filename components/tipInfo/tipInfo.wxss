/* components/tipInfo/tipInfo.wxss */
.tip-box{
  background-color: rgba(255,213,217,1);
  height: 60rpx;
  line-height: 60rpx;
  padding: 0 30rpx;
  overflow: hidden;
  box-sizing: border-box;
  font-size: 28rpx;
}